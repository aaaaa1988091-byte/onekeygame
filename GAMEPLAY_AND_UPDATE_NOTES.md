# Cake Rain RNG 玩法與更新說明

## MD 維護規則

1. **不得覆蓋整份文件**：更新時只能追加或修改相關段落，避免刪除既有玩法、架構與更新紀錄。
2. **更新必須保留三個區塊**：`玩法總覽`、`架構說明`、`更新與修復紀錄` 不可移除或改名。
3. **新增系統要同步記錄**：每次新增技能、轉盤詞條、UI、商店項目或資料欄位，都要在本文件補上用途與維護位置。
4. **腳本歸屬要寫清楚**：技能數值應在自己的 SkillScript，轉盤詞條數值應在自己的 RewardScript；管理服務只做派發與流程控制。
5. **更新紀錄只追加最新項目**：新版本請在 `更新與修復紀錄` 頂部新增日期與摘要，不要重寫舊紀錄。
6. **MD 保持中文說明**：本文件是維護者閱讀用文件，請保留中文；只有遊戲內玩家可見文字維持英文來源字串，交由 Roblox 自行翻譯。

## 玩法總覽

Cake Rain RNG 是一個以「天降蛋糕、吞食蛋糕、累積抽獎資源」為核心循環的 Roblox 小遊戲。

1. 蛋糕會持續從空中掉落到玩家附近。
2. 玩家碰到自己擁有的蛋糕後會開始自動吞食。
3. 蛋糕血量歸零後會給予蛋糕積分與轉盤次數。
4. 稀有蛋糕有更高血量與獎勵，發光蛋糕會開啟技能抽卡。
5. 玩家使用轉盤次數抽取轉盤詞條，例如吞食速度、自動抽獎、轉盤加速與品階上限提升。
6. 玩家可用商店解鎖更多可抽內容。
7. 玩家可打開背包查看自己目前擁有與尚未解鎖的可抽技能與轉盤詞條。

## 架構說明

- `WheelConfig`：只存轉盤詞條的身份、權重、解鎖條件與對應腳本名稱。
- `SkillConfig`：只存技能卡的身份、權重、解鎖條件與對應腳本名稱。
- `RewardScripts`：每個轉盤詞條各自管理自己的稀有度數值、持續時間與效果套用。
- `SkillScripts`：每個技能各自管理自己的稀有度數值、冷卻、持續時間、參數與實際效果。
- `RewardService`：只負責依照 `ScriptName` 派發轉盤詞條，不存放大量詞條數值。
- `SkillService`：只負責依照 `ScriptName` 派發技能、建立狀態與週期執行，不存放大量技能數值。
- `StateService.GetProfile(player)`：統一取得玩家狀態資料，其他服務可用它讀寫玩家資料，避免散落多套取得資料的方法。
- `StateService.BuildInventory(player)`：建立背包顯示資料，將可抽轉盤詞條與技能依解鎖狀態整理給客戶端。
- `LocalizationConfig`：只保留英文來源字串；遊戲內不硬編中文或其他語言，非英文顯示交給 Roblox 翻譯/本地化處理。

## 更新與修復紀錄

### 2026-08-27：莫蘭迪 UI、共用抽獎音效、落地關閉軌跡與 Lucky 詞條

- 按鈕與抽獎 Tick 改共用普通按鈕音效，避免抽獎音效和按鈕音效風格割裂。
- UI 主色調改為較明亮的莫蘭迪色系，文字改深色對比以提升可讀性並避免介面過黑。
- 蛋糕生成來源改支援 `ReplicatedStorage.cake`，執行時蛋糕放在 `Workspace.cake`；生成參數改為更密集但較近的雨落節奏：最小 6.56、最大 30、高度 40、存活 20 秒、間隔 0.2 秒、初始 3 顆。
- 蛋糕落地後會關閉隕落軌跡，技能 API 追加動量變更，技能模板可透過伺服器統一改位置、血量與動量。
- 新增預設 Lucky 轉盤詞條，會暫時提升蛋糕升階機率與抽獎高稀有權重。

### 2026-08-27：轉盤 Tick 改為中心線偵測並固定整數血量

- 抽獎主轉盤音效改為偵測目前穿過中心線的 Slot 索引，隨 Tween 實際位移與放慢曲線自然觸發，不再用固定間隔模擬。
- 蛋糕扣血統一在伺服器四捨五入，`Health` 屬性只寫入整數，避免百分比技能留下浮點血量。

### 2026-08-27：修正音效延遲、音量與轉盤節奏

- UI 音效改為啟動時建立模板並預載，點擊時 Clone 已載入音效，降低普通按鈕、購買與升級播放延遲。
- 提高按鈕、互動、轉盤 Tick 與蛋糕縮小音量，避免回饋過小。
- 蛋糕縮小音效改掛到透明世界錨點，不再跟著蛋糕吸收動畫透明或 Destroy 而中斷。
- 轉盤 Tick 間隔改由動畫時間與跨過欄位數推算，轉盤放慢時 Tick 也會同步變慢。

### 2026-08-27：UI 音效、視覺優化與技能蛋糕更新統一

- 新增四組音效設定：蛋糕縮小、抽獎轉盤 Tick、通用互動以及普通按鈕音效。
- HUD、商店、背包與轉盤增加漸層、描邊與更柔和的圓角，強化整體 UI 層次。
- 新增 `CakeService.ApplyServerCakeChange(player, cake, change)`，讓技能透過伺服器統一調整蛋糕血量與位置。
- 技能傷害改走與玩家吃蛋糕相同的扣血、刷新血量、縮放、獎勵與 Finish 流程，避免簡血與吃蛋糕機制不一致。

### 2026-08-25：追加時效轉盤等級、能力上限與神秘商人

- 修正轉盤等級為時效性詞條，依稀有度提供暫時等級加成，不再永久累加。
- 詞條與技能改為優先隊列倒數：最高階生效，低階保留剩餘時間並暫停，待高階結束後接續。
- 新增玩家速度轉盤詞條，並讓玩家速度由詞條效果動態調整。
- 背包新增能力等級與升級操作，能力等級會限制可抽到的最高品質。
- 商店會隱藏已購項目，並新增全伺服器每 4 分鐘刷新 3 個商品的神秘商人池。
- 發光蛋糕抽卡改為銀色轉盤 UI，並新增普通/銀色轉盤切換按鈕與周邊轉盤透明基準點。
- 新增 `StateService.GetLeaderboardSnapshot()`，方便後續全服排行榜讀取目前玩家分數快照。


### 2026-08-25：轉盤、堆疊、背包與存檔完整性重構

- 轉盤 5 格顯示改用角度與半徑的圓形座標配置，避免文字與格子擠在中心或邊緣。
- 轉盤詞條與技能堆疊改為高等級/高稀有度優先；高等級有效時會壓制低等級，新的高等級會移除同類低等級效果。
- 蛋糕血量造成的大小變化改為短時間漸變，並在變大/變小時播放粒子特效，提升回饋感。
- 商店與背包統一方形格狀捲軸，詞條與技能都帶 Icon；背包只顯示玩家已擁有或預設擁有的內容，不再列出未解鎖項目。
- 玩家資料序列化新增 ActiveBuffs、OwnedItems 與 Stats，並以剩餘時間還原使用中的詞條/技能；伺服器每 60 秒自動保存一次，離線與關服時也會保存。


### 2026-08-25：清理蛋糕生成符號問題與可讀性

- 修正蛋糕生成流程中 `warn(...) return` 同行造成的潛在 Luau 符號/語法問題，改成清楚的多行 guard clause。
- 拆出 `spawnPositionNear(root)`，讓生成位置計算集中管理。
- 展開 `HookTouches`、`Count`、`SpawnNear`、`StartPlayer` 與 `CleanupPlayer` 的壓縮單行程式，讓 CakeService 邏輯更容易維護與檢查。

### 2026-08-25：修正 MD 語言範圍，保留文件中文

- 依照維護需求將本文件改回中文，避免把「遊戲內英文單語系」誤套用到 MD 文件。
- 補充規則：MD 維護文件保持中文；遊戲內玩家可見文字才維持英文來源字串並交給 Roblox 翻譯。

### 2026-08-25：遊戲內文字改為英文來源，交由 Roblox 翻譯

- 移除內建繁體中文遊戲文字表，執行期間統一讀取英文來源字串。
- 將 HUD、提示、狀態欄、按鈕、輸出訊息與效果文字改為英文。
- 明確記錄：遊戲內玩家可見文字不再硬編非英文翻譯，後續語系交由 Roblox 翻譯/本地化處理。

### 2026-08-25：修復狀態欄未顯示完整以及修復錯誤的腳本架構，讓遊戲明了亦維護

- 修復底部狀態欄用途，轉盤抽到的詞條名稱、品階與效果/層數會顯示在底部讀條。
- 新增背包按鈕與能力背包面板，玩家可以查看自己擁有或尚未解鎖的可抽技能與轉盤詞條。
- 新增 `StateService.GetProfile(player)` 統一玩家資料取得函數，方便其他系統調用玩家資料。
- 新增 `StateService.BuildInventory(player)`，讓背包資料由伺服器統一整理後推送給玩家。
- 重構轉盤詞條腳本架構：吞食速度、發光蛋糕率、自動抽獎、轉盤加速等數值移回各自 RewardScript。
- 重構技能腳本架構：勾索、龍捲風、螞蟻運輸隊、蛋糕吸引的稀有度數值、持續時間、冷卻與參數移回各自 SkillScript。
- 保留服務腳本的管理責任：`RewardService` 與 `SkillService` 只做派發、啟動與流程控制，降低日後更新互相覆蓋或混亂的風險。
